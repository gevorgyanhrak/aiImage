import { mockRelatedLandings } from '@/mocks/data';

import type { SectionItem } from '@/types/sectionItem';

export const getRelatedLandings = (): SectionItem[] => {
  return mockRelatedLandings;
};
